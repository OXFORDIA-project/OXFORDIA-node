{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "stat-plugin_server",
  "projectType": "library",
  "sourceRoot": "packages/stat-plugin_server/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "cwd": "{projectRoot}",
        "command": "npm run build"
      }
    }
  }
}
