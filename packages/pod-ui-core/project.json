{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "pod-ui-core",
  "projectType": "library",
  "sourceRoot": "packages/pod-ui-core/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "cwd": "{projectRoot}",
        "command": "npm run build"
      }
    }
  }
}
