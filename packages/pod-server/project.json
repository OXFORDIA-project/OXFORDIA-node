{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "pod-server",
  "projectType": "library",
  "sourceRoot": "packages/pod-server/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist",
        "{projectRoot}/dist/components"
      ],
      "options": {
        "cwd": "{projectRoot}",
        "command": "npm run build"
      }
    }
  }
}
