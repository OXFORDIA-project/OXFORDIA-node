{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "stat-plugin-mean_server",
  "projectType": "library",
  "sourceRoot": "packages/stat-plugin-mean_server/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "cwd": "{projectRoot}",
        "command": "npm run build"
      }
    }
  }
}
