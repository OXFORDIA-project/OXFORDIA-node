# `stat-plugin-mean_server`

Mean statistic API plugin.

Concepts:
- Implements the server `StatisticApiPlugin` for mean queries.
- Reuses shared graph-path/SPARQL utilities from `stat-plugin_server`.

Example:
```ts
import { meanPlugin } from "@oxfordia/stat-plugin-mean_server";
```
