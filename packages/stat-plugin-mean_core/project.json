{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "stat-plugin-mean_core",
  "projectType": "library",
  "sourceRoot": "packages/stat-plugin-mean_core/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist",
        "{projectRoot}/src/generated"
      ],
      "options": {
        "cwd": "{projectRoot}",
        "command": "npm run build"
      }
    }
  }
}
