{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "pod-server-core",
  "projectType": "library",
  "sourceRoot": "packages/pod-server-core/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "cwd": "{projectRoot}",
        "command": "npm run build"
      }
    }
  }
}
