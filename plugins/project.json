{
  "name": "plugins",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "plugins",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build --workspace plugins"
      }
    },
    "build:ldo": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build:ldo --workspace plugins"
      }
    }
  }
}
