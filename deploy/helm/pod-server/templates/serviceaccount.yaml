{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pod-server.serviceAccountName" . }}
  labels:
    {{- include "pod-server.labels" . | nindent 4 }}
{{- end }}
