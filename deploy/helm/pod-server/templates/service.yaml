apiVersion: v1
kind: Service
metadata:
  name: {{ include "pod-server.fullname" . }}
  labels:
    {{- include "pod-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pod-server.selectorLabels" . | nindent 4 }}
